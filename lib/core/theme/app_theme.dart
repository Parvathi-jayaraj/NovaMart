import 'package:flutter/material.dart';

// Global app colors, fonts, button styles
class AppTheme {
  static ThemeData lightTheme = ThemeData(
    primaryColor: Colors.indigo,
    scaffoldBackgroundColor: Colors.white,
  );
}